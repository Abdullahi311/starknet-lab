[project]
name = "starknet-lab"
authors = ["SentimentPulse Team"]
description = "Real-time sentiment analysis and feedback aggregation platform"
telemetry = false
requirements = []
[contracts.sentiment-pulse]
path = "contracts/sentiment-pulse.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
